name: Website CI
on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-website-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # 质量门禁抽到可复用 workflow，与 website-release.yml 发版前门禁共用同一份定义
  quality:
    uses: ./.github/workflows/website-quality.yml
